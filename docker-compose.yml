version: '3.8'

services:
  visionware-ai:
    build: .
    container_name: visionware-ai-system
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
    volumes:
      - ./database:/app/database
